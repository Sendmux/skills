name: Plugin bundles

on:
  pull_request:
    paths:
      - "skills/**"
      - "openclaw.skills.json"
      - "assets/**"
      - ".claude-plugin/**"
      - ".agents/plugins/**"
      - ".cursor-plugin/**"
      - "mcp.json"
      - ".mcp.json"
      - "README.md"
      - "plugins/sendmux/**"
      - "scripts/build-plugin-bundles.mjs"
      - "scripts/check-plugin-bundles.mjs"
      - "scripts/build-plugin-bundles.test.mjs"
      - "scripts/agent-storage-floor.test.mjs"
      - "scripts/publish-openclaw-bundle.mjs"
      - "scripts/publish-openclaw-bundle.test.mjs"
      - "scripts/workflow-boundaries.test.mjs"
      - "scripts/check-workflow-path-filters.mjs"
      - ".github/workflows/openclaw-clawhub.yml"
      - ".github/workflows/notify-site.yml"
      - ".github/workflows/skill-drift.yml"
      - ".github/workflows/plugin-bundles.yml"
  push:
    branches:
      - main
    paths:
      - "skills/**"
      - "openclaw.skills.json"
      - "assets/**"
      - ".claude-plugin/**"
      - ".agents/plugins/**"
      - ".cursor-plugin/**"
      - "mcp.json"
      - ".mcp.json"
      - "README.md"
      - "plugins/sendmux/**"
      - "scripts/build-plugin-bundles.mjs"
      - "scripts/check-plugin-bundles.mjs"
      - "scripts/build-plugin-bundles.test.mjs"
      - "scripts/agent-storage-floor.test.mjs"
      - "scripts/publish-openclaw-bundle.mjs"
      - "scripts/publish-openclaw-bundle.test.mjs"
      - "scripts/workflow-boundaries.test.mjs"
      - "scripts/check-workflow-path-filters.mjs"
      - ".github/workflows/openclaw-clawhub.yml"
      - ".github/workflows/notify-site.yml"
      - ".github/workflows/skill-drift.yml"
      - ".github/workflows/plugin-bundles.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plugin-bundles-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Check Cursor, Claude, and Codex plugin bundles
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Test plugin bundle and workflow safeguards
        run: |
          node --test scripts/build-plugin-bundles.test.mjs \
            scripts/agent-storage-floor.test.mjs \
            scripts/publish-openclaw-bundle.test.mjs \
            scripts/workflow-boundaries.test.mjs

      - name: Check workflow path filters
        run: node scripts/check-workflow-path-filters.mjs

      - name: Check generated plugin bundles
        run: node scripts/check-plugin-bundles.mjs
